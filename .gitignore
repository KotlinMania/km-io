build
bin
.idea/*
*.iml
.gradle
*.swp
.project

.kotlin

kotlinx-io-native/repo/

# Project-local Android SDK (auto-installed by build.gradle.kts).
.android-sdk
local.properties

!.idea/vcs.xml
!.idea/dictionaries
!.idea/dictionaries/*
!.idea/codeStyles
!.idea/codeStyles/*

.gradletasknamecache
.settings
.classpath

logs


# CI proxy log captured by local ./gradlew build runs; not part of the source tree.
/build-log.txt

# macOS Finder metadata. These get sprinkled into directories as a side effect
# of opening folders in Finder; never part of the source tree.
.DS_Store
